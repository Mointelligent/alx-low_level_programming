#include<stdlib.h>

/**
 * _abs - This programs computes the absolute value of an integer.
 * @n: The value of the integer is convverted to an absolute value.
 *
 * Return: value
 */

int _abs(int n)
{
	return (abs(n));
}
